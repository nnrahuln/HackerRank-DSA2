# Insert a Node at the Head of a Linked List

## Problem

Given the head of a singly linked list, insert a new node before the current head.

The new node should contain the given data value and become the new head of the list.

## Example

Before insertion:

```text
383 -> 484 -> 392 -> NULL